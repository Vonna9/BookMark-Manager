/* Can add context menu to directly organize bookmarks from the file
chrome.contextMenus.onClicked.addListener(genericOnClick);
chrome.contextMenus.create({
    title: 'radio'
    type: 'radio'
    id:'radio'
}) */
    